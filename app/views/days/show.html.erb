<p id="notice"><%= notice %></p>

<table>
  <tr>
  	<% Day.column_names.each do |column_name| %>
  	<th><%=h column_name %></th>
  	<% end %>
  </tr>
  <tr>
  <% Day.column_names.each do |column_name| %>
 	<td><%=h @day[column_name] %></td>    	
  <% end %>  	
  </tr>
</table>

<%= link_to 'Upravit', edit_day_path(@day) %> |
<%= link_to 'Zpět', days_path+'?t='+@day.TIMETABLE_ID.to_s %>
