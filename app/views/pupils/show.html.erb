<p id="notice"><%= notice %></p>

<table>
  <tr>
  	<% Pupil.column_names.each do |column_name| %>
  	<th><%=h column_name %></th>
  	<% end %>
  </tr>
  <tr>
  <% Pupil.column_names.each do |column_name| %>
  	<% if column_name == "BIRTH_DATE" %>
  	  <td><%=h @pupil[column_name].strftime("%d.%m.%Y") %></td>
  	<% else %>
  	  <td><%=h @pupil[column_name] %></td>  
  	<% end %>
  <% end %>  	
  </tr>
</table>

<%= link_to 'Upravit', edit_pupil_path(@pupil) %> |
<%= link_to 'Zpět', pupils_path %>
