<h1>Škola</h1>

<table>  

<% @schools.each do |school| %>
  <tr>
  	<th>Název školy</th>
  	<th>ředitel</th>
  </tr>	
  <tr>
  	<td><%=h school.NAME %></td>
  	<td><%=h school.HEADMASTER %></td>
    <td><%= link_to 'Ukázat', school %></td>
    <td><%= link_to 'Upravit', edit_school_path(school) %></td>
    <td><%= link_to 'Smazat', school, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nová škola', new_school_path %>
